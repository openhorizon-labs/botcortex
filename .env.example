# Server-side account API; includes auth, inference, conversations and registry.
API_URL=http://localhost:8787

# Optional canonical production host. Vercel's production hostname is used otherwise.
# NEXT_PUBLIC_PRODUCTION_URL=your-production-host.example

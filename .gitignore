# Next.js / Node
node_modules/
.next/
out/
next-env.d.ts

# Local state
.env
.env*.local
*.local
.DS_Store
.claude/worktrees/

# TypeScript incremental build cache — regenerated on every build.
*.tsbuildinfo

# Browser-sim runtimes, copied from node_modules by `bun run vendor` on every
# build. 23 MB of binaries npm already versions — the wheel in
# public/botcortex/ IS committed, because it comes from another repo and a
# build must not silently pick up a different one than was tested.
/public/pyodide/
/public/mujoco/
/public/sim-worker.js
